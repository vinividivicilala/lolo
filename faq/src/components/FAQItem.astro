---
const { question, answer, label, date } = Astro.props;
---

<div class="faq-item-modal">
  <!-- Judul -->
  <div class="faq-box faq-title-box">
    <h2>{question}</h2>
  </div>

  <!-- Tanggal -->
  {date && (
    <div class="faq-box faq-date-box">
      <span class="faq-date">🕒 {date}</span>
    </div>
  )}

  <!-- Label -->
  {label && (
    <div class="faq-box faq-label-box">
      <span class="faq-label">{label}</span>
    </div>
  )}

  <!-- Deskripsi -->
  <div class="faq-box faq-description-box">
    <p>{answer}</p>
  </div>
</div>

<style>
.faq-item-modal {
  display: flex;
  flex-direction: column;
  gap: 18px; /* jarak antar box */
  padding: 20px;
  font-family: "Space Mono", monospace;
}

/* Semua box pakai line border */
.faq-box {
  border: 2px solid #000;       /* garis hitam */
  border-radius: 8px;           /* sudut agak membulat */
  padding: 16px 18px;
  background: #fff;
  box-shadow: 3px 3px 0 #000;   /* efek 3D retro */
}

/* Judul */
.faq-title-box h2 {
  margin: 0;
  font-size: 22px;
  font-weight: 700;
  text-transform: uppercase;
  color: #111;
}

/* Tanggal */
.faq-date {
  font-size: 15px;
  font-weight: 500;
  color: #333;
}

/* Label */
.faq-label {
  font-size: 14px;
  font-weight: bold;
  color: #fff;
  background: #d32f2f;
  padding: 4px 10px;
  border-radius: 4px;
}

/* Deskripsi */
.faq-description-box p {
  margin: 0;
  font-size: 16px;
  line-height: 1.7;
  color: #222;
}
</style>
