---
const { title = "Note Farid", description = "" } = Astro.props;
---

<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        background-color: #000;
        color: white;
        font-family: 'Poppins', sans-serif;
        min-height: 100vh;
      }

      /* Judul Website */
      .site-header {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        padding: 20px;
        font-size: 1.8rem;
        font-weight: 700;
        letter-spacing: 1px;
      }
      .site-header img {
        width: 35px;
        height: 35px;
      }

      /* Banner Baru */
      .custom-banner {
        background: #A0D2EB;
        color: #000;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 15px;
        padding: 15px 25px;
        margin: 0 auto 20px;
        border-radius: 12px;
        max-width: 700px;
        font-weight: 500;
        box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      }
      .custom-banner .left {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 1.1rem;
      }
      .custom-banner button {
        background: none;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        color: #000;
      }
    </style>
  </head>
  <body>
    <!-- Judul Website -->
    <header class="site-header">
      <img src="/images/logo.png" alt="Logo Note Farid" />
      <span>Note Farid</span>
    </header>

    <!-- Banner -->
    <div class="custom-banner" id="customBanner">
      <div class="left">👀 Note Farid</div>
      <button onclick="document.getElementById('customBanner').style.display='none'">✖</button>
    </div>

    <main>
      <slot />
    </main>
  </body>
</html>
